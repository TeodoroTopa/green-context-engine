# Publishing and editorial queue configuration

notion:
  # Energy Editorial Queue database (Notion Plus account)
  database_id: "33b15806d2a6814f9c59c673e4c878c8"
  # Status values: Review, Approved, Rejected, Published
  # Feedback: rich text field for rejection notes (filled by Teo)

# Pipeline pushes drafts to Notion automatically when NOTION_TOKEN is set.
# Without the token, drafts are saved locally only.
